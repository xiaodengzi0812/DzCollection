<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dengzi">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".tree.DoubleTreeActivity"></activity>
        <activity android:name=".tree.SearchTreeActivity"></activity>
        <activity android:name=".graph.GraphActivity"></activity>
        <activity android:name=".sort.SortActivity">
        </activity>
        <activity android:name=".algorithm.AlgorithmActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>